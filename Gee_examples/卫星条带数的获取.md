```python
import geemap
import ee
```



```python
import pandas as pd
#这里的年份要对应lansat的年份改

for year in range(1988,2012):
    START_DATE = f'{year}'
    END_DATE = f'{year + 1}'
    collection5 = ee.ImageCollection('LANDSAT/LT05/C01/T1_SR') .filterBounds(roi).filterDate(START_DATE, END_DATE ).map(cloudMaskL457).select(['B1', 'B2', 'B3', 'B4', 'B5', 'B7','pixel_qa'])
        .map(addLandsatVIs)
    path = pd.DataFrame(collection5.aggregate_array('WRS_PATH').getInfo())
 	path1 = path * 100
 # aggregate_array为获取属性的意思
# pd.dataframe为将属性和属性值转换为dataframe的意思
	row = pd.DataFrame(collection5.aggregate_array('WRS_ROW').getInfo())
    #add的意思是将path1与row相加
    merge = path1.add(row)
    #groupby的意思是将dartaframe根据条带来分组第0列为条带号）
    a = merge.groupby([0])
    #size为对groupby对象计算矩阵数量
    df1 = a.size()
    print(df1)
    print(collection5.size().getInfo())
    #最后一步其实是没必要的，但是可以拿来检验
```

