```python
ee_to_numpy(ee_object, bands=None, region=None, properties=None, default_value=None) 
#从图像中提取矩形区域的像素到每个波段的2D numpy数组中。
#bands：要提取的乐队名称列表。请确保所有波段具有相同的空间分辨率。默认为没有。
#properties：要从采样图像复制过来的属性。默认为所有非系统属性。
#default_value：当采样像素被遮罩或在带的足迹之外时使用的默认值。默认为没有。

```

