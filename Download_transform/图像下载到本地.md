```python
geemap.ee_export_image(ee_object, filename, scale=None, crs=None, region=None, file_per_band=False)
#scale:  像元大小 一个默认的尺度用于任何没有指定的波段;如果指定了CRS和crs_transform，则忽略。默认为没有。
#crs :默认CRS字符串，用于未显式指定的任何频带。默认为没有。
#region:指定要下载的区域的多边形;如果指定了CRS和crs_transform，则忽略。默认为没有。
#file_per_band = 是否为每个乐队制作不同的GeoTIFF。默认值为False。
```

