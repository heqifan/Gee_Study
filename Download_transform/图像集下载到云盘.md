```python
geemap.ee_export_image_collection_to_drive(ee_object, descriptions=None, folder=None, region=None, scale=None, crs=None, max_pixels=10000000000000.0, file_format='GeoTIFF', format_options={})
#descriptions：读的任务名称列表。
#folder：出文件所在的谷歌驱动器文件夹。默认为没有。
#region:区域。
#scale：分辨率(米/像素)。默认为图像分辨率的10倍。
#max_pixels：制导出中的像素数。默认为1.0 e13。
#file_format：将图像导出到的字符串文件格式。目前只支持'GeoTIFF'和'TFRecord'。默认为“GeoTIFF”。
```

