```python
ee_export_vector(ee_object, filename, selectors=None, verbose=True, keep_zip=False) 
#导出地球引擎FeatureCollection其他格式，包括shp, csv, json, kml，和kmz。
#selectors：要导出的属性列表。默认为没有。
#verbose：是否打印描述性文本。
#keep_zip：是否将下载的shapefile保存为zip文件。
```

```python
ee_to_shp(ee_object, filename, selectors=None, verbose=True, keep_zip=False)
#下载一个情感表达。FeatureCollection作为一个shapefile。
```

